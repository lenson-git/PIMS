# PIMS-Web

## 运行测试

本项目使用 [Vitest](https://vitest.dev/) 进行单元测试。

```bash
# 安装依赖
npm install

# 运行所有测试
npm test

# 运行特定测试文件
npx vitest tests/utils.test.js
```
 - 产品库存管理系统

基于 Supabase + GitHub Pages 的纯前端库存管理系统。

## 部署
```bash
git add .
git commit -m "Update"
git push origin main
```
